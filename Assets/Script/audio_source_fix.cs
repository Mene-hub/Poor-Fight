using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class audio_source_fix : MonoBehaviour
{
    // Start is called before the first frame update
    public AudioSource AS;
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
